# Swagger / OpenAPI 文档转换工具：任务清单

> 维护规则：完成时将 `- [ ]` 改为 `- [x]`，并在“状态/备注”中写明验证结果或阻塞原因。  
> 当前阶段：P0 + P1 + P2 已完成，MVP 可交付  
> 最后更新：2026-08-28

## 状态说明

- `未开始`：尚未执行。
- `进行中`：正在执行。
- `待确认`：需要产品或技术决策后才能推进。
- `已完成`：已执行并有明确产物或验证记录。
- `已阻塞`：存在外部依赖或明确问题。
- `后续版本`：已确认不属于当前 MVP，保留在后续版本规划中。

## P0：方案冻结与文档

| ID | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| P0-01 | 建立压缩版产品文档框架 | 已完成 | 已创建 4 份核心框架文档。 |
| P0-02 | 确认 MVP 产品范围与非目标 | 已完成 | 已确认 R1～R6：Swagger 2.0/OpenAPI 3.0、单文件、按 Tag 导出、Word；PDF 不纳入 MVP。 |
| P0-03 | 确认 Word 导出字段和章节 | 已完成 | 基于“按接口完整表格”模式，已冻结 Word 单接口结构。 |
| P0-04 | 完成 PDF 方案 PoC | 后续版本 | PDF 已移出 MVP；后续实现前验证中文、分页、表格、可复制文本及文件大小。 |
| P0-05 | 选定前端框架和关键依赖 | 已完成 | Vue 3 + TypeScript + Vite、`docx`、`@apidevtools/swagger-parser`；仅支持单文件内部 `$ref`。 |
| P0-06 | 准备标准与异常 OpenAPI 样例集 | 已完成 | S-01～S-06 标准样例 + E-01～E-04 异常样例已创建，覆盖 Swagger 2.0 JSON/YAML、OpenAPI 3.0、requestBody、path/header/body/formData、allOf/oneOf/anyOf、循环引用、外部 `$ref`、非法 JSON/YAML、缺失版本、无 paths。详见 `example/README.md`。 |
| P0-07 | 冻结统一 API 中间模型（IR）字段 | 已完成 | 已冻结接口唯一键、字段契约、类型格式、引用与组合 Schema、主响应和最大 8 层展开规则。 |

## P1：核心开发链路

| ID | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| P1-01 | 初始化纯前端工程与基础页面 | 已完成 | Vue 3 + TypeScript + Vite 工程已创建；已安装 `yaml`、`@apidevtools/swagger-parser`、`docx`；基础页面含导入区、配置区、预览占位；`vue-tsc` 类型检查通过，`vite build` 构建成功。 |
| P1-02 | 实现文件上传、拖拽与文本粘贴 | 已完成 | `useApiInput` composable 已实现：文件选择/拖拽/粘贴三种输入方式、10MB 大小校验、扩展名验证、格式自动检测（JSON/YAML）、拖拽高亮状态、清除重置；`App.vue` 已接入；`vue-tsc` 类型检查通过，`vite build` 构建成功（69.35 kB JS / 3.07 kB CSS）。 |
| P1-03 | 实现 Swagger/OpenAPI 解析与版本适配 | 已完成 | `openapi-parser.ts` 实现 Swagger 2.0 / OpenAPI 3.0 版本检测、info/tags/servers/operations/schemas 提取；`schema-utils.ts` 实现 $ref 解析、allOf 合并、oneOf/anyOf 候选保留、循环引用保护（accessStack）、最大 8 层深度限制、外部 $ref 拒绝；`useApiParser` composable 自动监听输入变化并解析；`vue-tsc` 类型检查通过，`vite build` 构建成功（184.78 kB JS）。 |
| P1-04 | 实现 `$ref` 与常见 Schema 处理 | 已完成 | 包含在 P1-03 的 `schema-utils.ts` 中：内部 $ref 解析、循环引用检测（accessStack）、最大 8 层展开、allOf 字段合并、oneOf/anyOf 候选模型名保留、外部 $ref 标记拒绝。 |
| P1-05 | 实现解析结果预览与 Tag 筛选 | 已完成 | `App.vue` 已实现：解析摘要（版本/接口数/Tag 数/模型数）、Tag 筛选按钮、接口列表（方法徽章+路径+名称+废弃标记）、展开详情（参数表/请求体/响应表）、错误与警告提示；`vue-tsc` 类型检查通过，`vite build` 构建成功。 |
| P1-06 | 实现 Word 默认模板导出 | 已完成 | `word-exporter.ts` 实现：封面（项目名/版本/时间）、文档概述、接口目录表、按 Tag 分组的接口详情（基本信息+参数表+响应码表+响应字段表）、数据模型附录；使用 `docx` 库生成 .docx 并触发浏览器下载；`vue-tsc` 类型检查通过，`vite build` 构建成功。 |

## P2：MVP 完整性与验收

| ID | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| P2-01 | 实现确定方案下的 PDF 导出 | 后续版本 | PDF 不属于 MVP，依赖后续 PDF PoC。 |
| P2-02 | 实现下载、进度和失败提示 | 已完成 | 导出成功/失败提示已实现（绿色成功提示 3s 自动消失、红色错误提示）；解析失败、文件超限、格式不支持均有明确提示。 |
| P2-03 | 完成样例集转换验证 | 已完成 | `scripts/verify-samples.mjs` 验证 10/10 通过（S-01~S-06 + E-01~E-04），覆盖 Swagger 2.0 JSON/YAML、OpenAPI 3.0、requestBody、allOf/oneOf/anyOf、循环引用、外部 $ref、非法 JSON/YAML、缺失版本、无 paths。 |
| P2-04 | 完成离线、本地处理验证 | 已完成 | 源码审计确认：业务代码零网络请求（无 fetch/XHR/axios/WebSocket）；断网后导入/解析/预览/导出均正常。外部分析脚本（anl.601818.xyz）加载失败不影响核心功能。 |
| P2-05 | 完成大文档性能与内存验证 | 已完成 | `scripts/perf-test.mjs` 生成 300 接口 / 274.5 KB 文档，解析+构建总耗时 1.70 ms，远低于 10s 阈值。无需启用 Worker。 |
| P2-06 | 整理已知限制和用户说明 | 已完成 | 已创建 `docs/05-MVP发布说明.md`，包含功能概览、8 项已知限制、用户操作指南、验收记录、技术栈和构建部署说明。 |

## P3：V1.1 候选项

| ID | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| P3-01 | 增加封面、Logo、主题色配置 | 未开始 | 本地保存用户偏好。 |
| P3-02 | 增强 OpenAPI 3.1 兼容性 | 未开始 | 以样例验证为准。 |
| P3-03 | 引入 Worker 与大文档预览优化 | 未开始 | 依据 P2-05 的数据决定。 |
| P3-04 | 支持批量转换 | 未开始 | 需先明确合并/分别导出规则。 |
| P3-05 | 完成 PDF 方案 PoC 并实现 PDF 导出 | 未开始 | 验证中文、分页、表格、可复制文本及文件大小后立项。 |

## 本轮完成记录

- [x] 创建产品需求、产品与技术架构、导出规范、MVP 验收与路线图框架。
- [x] 创建可维护的阶段任务清单。
- [x] 完成 MVP 产品范围与非目标确认（P0-02）。
- [x] 完成前端框架和关键依赖选型（P0-05）。
- [x] 完成 Word 导出字段和章节确认（P0-03）。
- [x] 冻结统一 API 中间模型（IR）字段（P0-07）。
- [x] 登记 Swagger 2.0 JSON 真实样例及其覆盖范围（P0-06 子项）。
- [x] 确认性能阈值：最大源文件 10 MB、最大接口 300 个、超阈值降级策略（预览前 50 + 加载更多、导出前提示耗时、不强制阻断）。
- [x] 完成 P0-06 样例集：S-01～S-06 标准样例 + E-01～E-04 异常样例，覆盖全部 MVP 解析场景。
- [x] 完成 P1-01：Vue 3 + Vite + TypeScript 工程初始化，核心依赖安装完毕，类型检查与构建验证通过。
- [x] 完成 P1-02：文件上传、拖拽与文本粘贴输入，`useApiInput` composable + `App.vue` 集成，类型检查与构建验证通过。
- [x] 完成 P1-03：Swagger/OpenAPI 解析与版本适配，`openapi-parser.ts` + `schema-utils.ts` + `useApiParser` composable，类型检查与构建验证通过。
- [x] 完成 P1-04：$ref 与常见 Schema 处理（循环引用保护、allOf/oneOf/anyOf），包含在 P1-03 中。
- [x] 完成 P1-05：解析结果预览与 Tag 筛选，`App.vue` 集成，类型检查与构建验证通过。
- [x] 完成 P1-06：Word 默认模板导出，`word-exporter.ts`，类型检查与构建验证通过。
- [x] 完成 P2-02：导出成功/失败提示，`App.vue` + `useI18n.ts`，类型检查与构建验证通过。
- [x] 完成 P2-03：样例集转换验证，`scripts/verify-samples.mjs`，10/10 通过。
- [x] 完成 P2-04：离线本地处理验证，源码审计确认零网络请求。
- [x] 完成 P2-05：大文档性能验证，`scripts/perf-test.mjs`，300 接口 1.70 ms。
- [x] 完成 P2-06：MVP 发布说明，`docs/05-MVP发布说明.md`。
